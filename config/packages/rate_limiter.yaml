framework:
    rate_limiter:
        test_submission:
            policy: 'fixed_window'
            limit: 1
            interval: '1 minute'

        test_view:
            policy: 'fixed_window'
            limit: 1
            interval: '1 minute'