{% extends 'base.html.twig' %}

{% block title %}ReponsesScore{% endblock %}

{% block body %}
    <h1>ReponsesScore</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponses_score.id }}</td>
            </tr>
            <tr>
                <th>Question_id</th>
                <td>{{ reponses_score.questionId }}</td>
            </tr>
            <tr>
                <th>Lettre_reponse</th>
                <td>{{ reponses_score.lettreReponse }}</td>
            </tr>
            <tr>
                <th>Points</th>
                <td>{{ reponses_score.points }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reponses_score_index') }}">back to list</a>

    <a href="{{ path('app_reponses_score_edit', {'id': reponses_score.id}) }}">edit</a>

    {{ include('reponses_score/_delete_form.html.twig') }}
{% endblock %}
